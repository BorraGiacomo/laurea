\begin{frame}[fragile]{Equilibrio di Nash}

\begin{columns}[c]

    \column{0.5\textwidth}

    \begin{itemize}
        \item Analiticamente:\\Teorema del Punto Fisso di Brouwer
        \item Numericamente:\\Iterazioni Controllate
    \end{itemize}

    \column{0.5\textwidth}

    \begin{lstlisting}[style=myStile]
def f_hat(...):
    composition = phi(
        T_hat(...)
    )

    return self.normalize(
        theta_hat
        - self.lmbda * (
            composition
            - (theta_hat.T @ composition)[0]
              * self.one_n_hat
        )
    )
\end{lstlisting}

\end{columns}

\end{frame}